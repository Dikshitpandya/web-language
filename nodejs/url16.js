var http = require ("http");
var fs = require("fs");
const { response } = require("express");
var server = http.createserver(function(request ,response)){
      
}
server.listen(5000);
console.log("server is reiuing in port no.5000")